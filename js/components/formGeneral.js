export const formGeneral = function() {
	$( document ).ready(() => {
		// $('#checkbox-label').on('click', function(event) {
		// 	const isChecked = $('#checkbox').attr('checked');
		// 	if (typeof isChecked === typeof undefined || isChecked === false) {
		// 		$('#checkbox').attr('checked', 'true')
		// 	} else $('#checkbox').removeAttr('checked');
		// })
	})
}